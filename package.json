{
  "name": "chat-app-root",
  "version": "1.0.0",
  "description": "Real-time chat app — root workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" --kill-others-on-fail \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "start": "npm run dev",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
